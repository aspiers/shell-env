#!/bin/bash

confdir=.cron.d
config=$HOME/.crontab

source "$ZDOTDIR/lib/libhooks.sh"

rebuild_config "$config" "$confdir"

if ( echo; cat "$config" ) | crontab -; then
    echo "Rebuilt user's crontab"
else
    echo "WARNING: Failed to rebuild user's crontab" >&2
fi

