#!/bin/bash

~/bin/service-commands
~/bin/cm

local_hook=$HOME/.zsh/functions/cx_hook_local
if ! [ -e "$local_hook" ]; then
    cp "$local_hook.tmpl" "$local_hook"
    chmod +x "$local_hook"
    echo "Created $local_hook from template"
fi

if which zsh >&/dev/null; then
  zsh -c zrec
fi

