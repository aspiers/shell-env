#compdef about packck pvcsbinlinks pvcsmanlinks use unuse

local expl

if (( ! $+PVCS_packlist )) && which packlist >/dev/null; then
  $PVCS_packlist=( $(packlist) )
fi

_description expl 'Packages under PVCS'
compadd "$expl[@]" - $PVCS_packlist
