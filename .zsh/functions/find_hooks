#autoload

. $ZDOTDIR/.zsh/functions/run_hooks DEFUN # this will only load the functions we need

iterate_hooks find "$@"

. $ZDOTDIR/.zsh/functions/enable_wordsplit

if [ -n "$hooks_found" ]; then
    #echo "$hooks_found"
    for hook in $hooks_found; do
        echo $hook
    done
fi

restore_wordsplit