#!/bin/bash

beep

if [ -n "$DISPLAY" ]; then
    if which zenity >& /dev/null; then
        zenity --info --text "$*" && exit 0
    fi

    if which xmessage >/dev/null 2>&1; then
        xmessage "$*" && exit 0
    fi
fi

# getting desperate
echo "$*" | write `id -un`
logger "$0: $*"
