#!/bin/sh

if [ -z "$1" ]; then
  echo "Usage: $0 <image file> [ <degrees> ]"
  exit 1
fi

file="$1"
angle=${2:-90}
newfile="${file}.$angle"

jpegtran -rotate $angle -copy all "$file" > "$newfile" \
  && mv "$newfile" "$file"
