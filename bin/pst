#!/bin/sh

case "$OSTYPE" in
  solaris*) exec ptree     "$@" | less -rS ;;
         *) if which pstree 2>&1 >/dev/null; then
              pstree -A -p "$@" | less -rS
            else
              echo "pstree not found."
              exit 1
            fi
            ;;
esac

