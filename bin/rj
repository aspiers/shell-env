#!/bin/sh

case "$OSTYPE" in
  solaris*) exec /usr/bin/ps -ef | egrep "($*|^ *UID)" | egrep -v "grep.+USER|$0|ps -ef\$"
            ;;

  # FreeBSD and Linux
         *) exec ps auxwwww -L | egrep "($*|^USER)"  | egrep -v "grep.+USER|$0|ps auxwwww\$"
            ;; 
esac
