#!/bin/sh

case "$OSTYPE" in
  solaris*) exec ps -ef      | grep -E "($*|^ *UID)" | grep -vE "grep|$0"
            ;;

  # FreeBSD and Linux
         *) exec ps -auxwwww | grep -E "($*|^USER)"  | grep -vE "grep|$0"
            ;; 
esac
